# Example environment variables
# Note: Variables prefixed with VITE_ are exposed to the client bundle by Vite.
# Treat VITE_* values as public. Do not put private backend secrets here.

# Public API keys (rate-limited; acceptable to expose in client)
VITE_EIA_API_KEY=
VITE_NREL_API_KEY=

# Private API keys (DO NOT expose in client)
# For providers like DeepSeek/OpenAI, use a server-side proxy and read keys from server env.
# DEEPSEEK_API_KEY=
