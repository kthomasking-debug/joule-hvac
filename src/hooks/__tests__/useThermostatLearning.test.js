/* @vitest-environment jsdom */
/**
 * Skipped duplicate of useThermostatLearning.test.jsx.
 * Original contained JSX in .js causing Vite parse error.
 * Coverage retained by .jsx file.
 */
import { describe, it } from "vitest";

describe.skip("useThermostatLearning duplicate (.js)", () => {
  it("skipped: duplicate file uses JSX, see .jsx version", () => {});
});
